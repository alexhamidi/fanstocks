import { UIntegration, UStock } from "../_models/types";

export const uIntegration: UIntegration = {
  service: undefined,
  community: undefined,
};

export const uStock: UStock = {
  ticker: "",
  names: undefined,
};
